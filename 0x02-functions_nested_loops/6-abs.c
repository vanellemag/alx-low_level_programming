#include "main.h"
/**
 * _abs - absolute value
 * @n: integer
 * function tha computes the absolute value of an integer
 * Return: success
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	if (n < 0)

		return (-n);
	return (0);
}
