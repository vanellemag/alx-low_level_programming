#include <elf.h>
/**
 * main - print elf
 * @argc: int
 * @argv: char
 * Return: alway 0
 */
int main(int argc, char *argv[])
{
	FILE *file = argv[1];
	if (argc != 2 || (file == NULL))
		return (-1);
	if (file == NULL)

}
